/*
 * AppRoute Messages
 *
 * This contains all the text for the AppRoute component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AppRoute';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AppRoute component!',
  },
});
